PORT=5173

# --- Opt-in agentic mode (ADR-003). Leave empty to keep canned replies. ---
# Auth uses your Azure CLI session: run `az login` first. No keys needed.
# Example endpoint: https://my-aoai.openai.azure.com
# Deployment is the *deployment name* of a gpt-*-mini model in your resource.
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_API_VERSION=2024-12-01-preview
